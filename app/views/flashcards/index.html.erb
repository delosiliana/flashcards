<section>
  <p style="font-size: 22px">
    Первый в мире удобный менеджер флеш-карточек. Именно так.
  </p>
</section>
<section>
<% if @card.present? %>
  <% if @card.picture %>
    <%= image_tag @card.picture.url, id: 'picture' %>
  <% end %>
  Переведенный текст: <%= @card.translated_text %>
  <%= form_tag check_original_text_card_card_path(@card.id) do %>
    <%= hidden_field_tag :id, @card.id %>
    <%= label :answer, 'Оригинальный текст' %>
    <%= text_field_tag :answer %>
    <%= submit_tag 'Проверить введеный текст' %>
  <% end %>
<% else %>
  <h2>Сегодня нет карточек для проверки<h2>
<% end %>
</section>
